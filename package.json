{
  "name": "jellyfish-climb",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/copy-sound-assets.mjs && node scripts/generate-seo-assets.mjs && node scripts/validate-site.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "validate:site": "node scripts/validate-site.mjs"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  }
}
